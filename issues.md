- [ ] 修复交易/退款分段记账导致的单边记账风险：第一阶段已扣款、第二阶段失败时不能直接终态失败，应保留可补偿状态或执行自动冲正。
- [x] 为退款增加原单前置校验：原单必须处于 `RECV_SUCCESS` 且业务类型可退款，避免对未完成或非法原单退款。
- [ ] 修复 Outbox claim 竞争窗口：改为原子 claim（例如 claim+标记 processing+returning）以降低重复投递风险。
- [ ] 收敛重复 webhook 轮询实例，避免同一事件被多 worker 并行处理放大重复投递概率。
- [x] 对齐文档与实现：统一退款接口返回字段（如 `origin_refundable_left`）和校验时机（同步/异步）语义。
- [x] 清理或整合未使用的退款实现（`internal/service/refund.go`），避免双实现并存带来的维护歧义。
- [ ] 优化退款账本恢复路径性能：避免每次退款都全量扫描历史账本/退款记录，改为增量或预聚合方案。
- [ ] 优化队列满载时的降级策略：避免在请求线程同步执行重处理，降低高峰期接口延迟抖动。
- [ ] 收敛状态枚举与数据库约束：既然未发布，直接将状态定义统一为 `INIT/PAY_SUCCESS/RECV_SUCCESS/FAILED`，同步更新 DDL/check 约束与相关 SQL，彻底移除 `PROCESSING` 残留。
- [ ] 对齐状态机变更的文档与运维配置：更新 API/功能设计/测试用例中的状态流转描述（去掉 `PROCESSING`），并清理或重命名遗留配置项与说明（如 stage worker/queue 文案），防止认知偏差与误配置。
